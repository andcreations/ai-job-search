if [ -n "$BASH_VERSION" ]; then
  # bash
  SCRIPT_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/$(basename "${BASH_SOURCE[0]}")"
elif [ -n "$ZSH_VERSION" ]; then
  # zsh
  SCRIPT_PATH="$(cd "$(dirname "${(%):-%x}")" && pwd)/$(basename "${(%):-%x}")"
else
  echo "Unsupported shell. Please use bash or zsh."
  exit 1
fi
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"

export JWT_SECRET="owRz8QogIYkN79Sw6WpTqJ@a"
export SYSTEM_ADMIN_TOKEN="t&tuf9F4oKi!Dtf#"

export SQLITE_DB_DIR="$SCRIPT_DIR/../_db"
export AI_JOB_SEARCH_WEB_APP_DIR="$SCRIPT_DIR/../../frontend/web-app/dist/app"

export DEFAULT_THREAD_CFG_NAME="default"